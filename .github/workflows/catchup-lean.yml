name: Catchup Lean

on:
  push:
    branches: [formalize-v18-manuscript-catchup]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: catchup-lean-v2-${{ github.ref }}
  cancel-in-progress: true

jobs:
  targeted:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: leanprover/lean-action@v1
        with:
          auto-config: false
          build: true
          test: false
          lint: false
          build-args: >-
            EverettianDecoherence.Approximation.StabilitySharpness.FixedBudgetDiagonal
            EverettianDecoherence.Approximation.OptimalTwoCellTail
            EverettianDecoherence.Approximation.StabilitySharpness.DiffuseFamilyTail
            EverettianDecoherence.Approximation.StabilitySharpness.DiffuseFamilyMaxCut