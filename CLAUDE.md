# Claude Code instructions

## Français

Lire intégralement `AGENTS.md` avant toute modification, puis le
`AGENTS.md` le plus proche du sous-répertoire visé. Exécuter
`scripts/validate.sh` avant de déclarer un incrément vert. Ne jamais modifier
les dépendances ni pousser sans instruction explicite.

## English

Read `AGENTS.md` completely before any change, then the closest applicable
`AGENTS.md`. Run `scripts/validate.sh` before declaring an increment green.
Never change dependencies or push without explicit instruction.
